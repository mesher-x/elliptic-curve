{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build - opencl",
            "type": "shell",
            "command": "g++ main.cpp -DCL_HPP_TARGET_OPENCL_VERSION=120 -DCL_HPP_MINIMUM_OPENCL_VERSION=120 -o executable -lOpenCL",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}